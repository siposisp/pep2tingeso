apiVersion: v1
kind: PersistentVolume
metadata:
  name: dbmsvehiculos-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/dbmsvehiculos-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dbmsvehiculos-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dbmsvehiculos-deployment
  labels:
    app: dbmsvehiculos-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: dbmsvehiculos-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: dbmsvehiculos-deployment
        tier: database
    spec:
      containers:
        - name: dbmsvehiculos
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_VEHICULOS
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_VEHICULOS
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: dbmsvehiculos-data
      volumes:
        - name: dbmsvehiculos-data
          persistentVolumeClaim:
            claimName: dbmsvehiculos-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: dbmsvehiculos-service
  labels:
    app: dbmsvehiculos-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: dbmsvehiculos-deployment
    tier: database
  type: LoadBalancer